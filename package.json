{
  "name": "planificador-abordaje-ifs",
  "version": "2.0.0",
  "private": true,
  "description": "Planificador de Abordaje IFS — Gestión de prospectos y agenda comercial (Supabase)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "serve public -l ${PORT:-3000} --no-clipboard",
    "check": "node scripts/check.mjs",
    "smoke": "node scripts/smoke.mjs",
    "test": "node scripts/test.mjs",
    "verify": "npm run check && npm run smoke && npm run test"
  },
  "dependencies": {
    "@babel/standalone": "7.29.7",
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@babel/standalone": "^7.29.7"
  }
}