{
  "name": "planificador-abordaje-ifs",
  "version": "1.0.0",
  "private": true,
  "description": "Planificador de Abordaje IFS — Gestión de prospectos y agenda comercial",
  "scripts": {
    "build": "staticrypt index.html -p \"$STATICRYPT_PASSWORD\" --short --template ifs_template.html -d public",
    "start": "serve public -l ${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "staticrypt": "^3.5.4",
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
